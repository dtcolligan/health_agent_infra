# Pilot Runs

Model-backed pilot orchestrator outputs are written here under dated run
directories. Generated run directories and the `latest` symlink are ignored by
git; this file and `.gitignore` keep the layout anchored.
