# Flagship loop README/demo correction table

| current_claim | observed_repo_truth | required_correction |
| --- | --- | --- |
| Top-level demo centers the whole flagship loop on `contract -> bundle init -> voice-note submit -> context get -> recommendation create`. | The approved proof-audit slice is narrower: the frozen recommendation proof must start from `tests/fixtures/agent_readable_daily_context/generated_fixture_day_context.json`, not from runtime bundle bootstrap or voice-note ingestion. | Present the flagship proof for this slice as a frozen context-to-recommendation audit path, and describe bundle bootstrap plus voice-note submit as adjacent CLI capabilities rather than the proof object itself. |
| The README demo writes only to `/tmp/health_lab_demo/...` and leaves the evaluator to inspect ad hoc local outputs. | This repo now needs a checked-in evaluator bundle with frozen payloads, generated recommendation artifacts, and a fail-closed preservation proof. | Point reviewers to `artifacts/flagship_loop_proof/2026-04-09/` as the canonical proof bundle, and keep `/tmp` paths as disposable runtime examples only. |
| Recommendation creation is described as real, but the README does not show one adjacent rejected case proving preexisting artifacts stay unchanged. | The CLI already fails closed on ungrounded evidence refs, and the approved slice requires reproducible byte-preservation proof, not just an error envelope. | Add the negative proof path and preservation artifact to the demo/readme story so trust claims stay evidence-backed. |
